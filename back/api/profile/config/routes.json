{
  "routes": [
    {
      "method": "POST",
      "path": "/profile",
      "handler": "profile.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/profile/:id",
      "handler": "profile.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}
