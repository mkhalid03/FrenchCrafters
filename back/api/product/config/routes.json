{
  "routes": [
    {
      "method": "GET",
      "path": "/products/:id",
      "handler": "product.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/products/check",
      "handler": "product.check",
      "config": {
        "policies": []
      }
    }
  ]
}
